## TODO

1. 현재 에이전트 메시지 별로 블락이 새로 생성되는데 연속된 도구호출 및 미드턴 대화 등이 ai상 하나로 보이게끔 (복사 버튼) 해야함
2. 실제로 구현되지 않은 "개선필요", "도움이됨" 버튼은 깔끔하게 다 제거하고 답변 완료 시각 보이게끔
3. 지금 생성된 아티팩트 블록이 채팅 중에 안보이고 새로고침 해야 채팅 최 하단에 보이는데 그게 아니라 ai가 아티팩트나 라이브러리 도구호출 다 하고 나면 그쪽에서 카드가 뜨도록
4. 서버 새로 시작 후에 이전 대화 목록은 뜨는데 막상 클릭하면 "대화를 불러오는 중" 만 계속 뜨면서 채팅 내용 안뜨는 문제
   - 이번 라운드에서는 제외
   - 프론트 fallback UX로 완화는 가능하지만, 실제 히스토리 복구 여부는 백엔드/LangGraph thread persistence 성격이 커서 별도 이슈로 분리

## AI DONE

- 1번 진행 완료
  - assistant 응답을 메시지 단위가 아니라 turn 단위로 그룹 렌더링하도록 변경
  - 연속된 AI 응답 / 도구 호출 / 최종 응답이 하나의 assistant turn 안에서 보이도록 정리
  - thinking / 도구 호출 과정도 turn 안에서 함께 렌더링되도록 구조 변경

- 2번 진행 완료
  - 사용자 피드백 버튼 UI 비노출 처리
  - assistant turn 하단에 복사 버튼과 시간 표시 footer 추가

- 3번 1차 진행 완료
  - 아티팩트 결과물이 turn 중간 흐름 안에서 보이도록 inline 카드 렌더링 추가
  - source_artifact_id로 연결 가능한 라이브러리 문서 카드도 turn 안에서 함께 보이도록 추가
  - turn에 연결되지 않은 기존 결과물은 별도 fallback strip로 유지

- 관련 보강
  - assistant turn 그룹핑 로직을 순수 함수로 분리하고 테스트 추가
  - chat-view 관련 렌더링/그룹핑 테스트 갱신

- 4번 무한로딩 이슈는 이번 라운드에서 제외
  - 별도 이슈로 유지
